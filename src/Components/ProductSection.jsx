import Loader from "./Loading";

const ProductSection = () => {
  return (
    <section className="w-full p-14 flex justify-center items-center border border-gray-300">
      <Loader />
    </section>
  );
};

export default ProductSection;
